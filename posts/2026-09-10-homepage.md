---
title: ホームページをつくりはじめました
date: 2026-09-10
summary: Pythonで、自分の日記を置けるホームページをつくっています。
tags:
  - 日記
  - プログラミング
  - Python
  - AtCoder
  - 100日でPython初心者卒業
---

明日から、#100日でPython初心者卒業 を目指します。

## Day 0：現在地と目標

現在地：AIにかなり頼っている

### 目標

- PCEP合格
- AtCoder茶色
- コードを自分で説明できるようになる

## この場所に置いていきたいもの

- Pythonを勉強していて気づいたこと、わからなかったこと
- ゲームや小さなプログラムの制作記録
- その日に考えたこと

完成したことだけでなく、途中の迷いや変化も残していく予定です。

## 今日やったこと

### ホームページ（Ten-Homepage）のコード理解

#### `PROFILE` からHTMLに表示されるまで

`content.py` の `PROFILE` は、プロフィール情報を入れた辞書。

```python
PROFILE = {
  "title": "点のホームページ",
}
```

`app.py` の `home()` で、`PROFILE` を `profile` という名前で `index.html` に渡している。

```python
render_template(
  "index.html",
  profile=PROFILE,
)
```

HTMLの `{{ profile.title }}` は、Pythonの次の処理に近い。

```python
PROFILE["title"]
```

データが表示されるまでの流れは次の通り。

```text
content.py の PROFILE
↓
home() で profile という名前にして index.html へ渡す
↓
{{ profile.title }} で表示する
```

`PROFILE` と `profile` は別の名前。
大文字の `PROFILE` はPython側のデータで、小文字の `profile` はテンプレート側で使う名前。

### AtCoder UniqueAC 13問

#### ABC062_A

状態：解説AC

##### つまずいたところ

- `if` 式の評価順
- `(x and y) in (l1 or l2)` は「`y` が `l1` にあるか」だけを判定する
- 想定の通り動かすには、次のように書く

  ```python
  (x in l1 and y in l1) or (x in l2 and y in l2)
  ```